node_modules/
dist/
build/
*.log

# env / secrets
.env
.env.*
!.env.example

# collector runtime data
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

data
*.pem
*.key

# agent build output
packages/agent/dist-bin/

# bun's own lockfile at root is a byproduct of building packages/agent;
# npm's package-lock.json is the monorepo's source of truth
/bun.lock

# editors / os
.DS_Store
.vscode/
.idea/
packages/agent/dev-config.json
